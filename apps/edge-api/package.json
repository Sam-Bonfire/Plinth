{
  "name": "edge-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "lint": "echo 'No TS files to lint in edge-api'",
    "test": "cargo test"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}

{
  "name": "edge-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "worker-build --dev",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "lint": "echo 'No TS files to lint in edge-api'",
    "test": "cargo test",
    "d1:migrations:apply": "wrangler d1 migrations apply plinth_cellar --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "wrangler": "^4.0.0"
  }
}
